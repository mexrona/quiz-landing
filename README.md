# Квиз-лендинг V1 - Учебный проект

Минимально рабочая версия интерактивного квиза на React. Идеально подходит для обучения основам React и веб-разработки.

## 📋 Структура проекта

```
quiz-landing/
├── src/
│   ├── components/
│   │   ├── Question.jsx      # Компонент вопроса
│   │   └── Results.jsx       # Компонент результатов
│   ├── App.jsx               # Главный компонент (логика квиза)
│   ├── App.css               # Стили приложения
│   ├── data.js               # Данные вопросов
│   └── main.jsx              # Точка входа приложения
├── public/
│   └── index.html            # HTML файл
├── package.json              # Зависимости проекта
├── vite.config.js            # Конфигурация Vite
└── README.md                 # Этот файл
```

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
npm install
```

### 2. Запуск в режиме разработки
```bash
npm run dev
```

Приложение откроется автоматически на `http://localhost:3000`

### 3. Сборка для продакшена
```bash
npm run build
```

## ✨ Возможности V1

- ✅ Каждый вопрос на отдельном экране
- ✅ Варианты ответов как интерактивные кнопки
- ✅ Индикатор прогресса ("Шаг X из Y")
- ✅ Визуальная полоса про прогресса
- ✅ Финальный экран с результатом
- ✅ Рекомендации на основе результатов
- ✅ Процент правильных ответов
- ✅ Кнопка для повтора квиза
- ✅ Адаптивный дизайн (мобильные, планшеты, десктоп)
- ✅ Красивая анимация

## 📱 Адаптивность

Приложение полностью адаптивно:
- **Десктоп** (> 768px) - полная версия с комфортным размером
- **Планшет** (768px) - оптимизировано для сенсорных экранов
- **Мобильный** (< 480px) - компактная версия

## 🔧 Как расширить проект

### Добавить новые вопросы
Отредактируйте файл `src/data.js`:
```javascript
export const quizData = [
  {
    id: 1,
    question: "Ваш вопрос?",
    options: ["Вариант 1", "Вариант 2", "Вариант 3", "Вариант 4"],
    correctAnswer: 0, // индекс правильного ответа
    category: "категория"
  },
  // добавьте еще вопросы
];
```

### Изменить рекомендации
В файле `src/data.js` найдите функцию `getRecommendation` и измените сообщения.

### Стилизовать приложение
Все стили в файле `src/App.css`. Легко менять:
- Цвета (измените `#667eea` и `#764ba2`)
- Размеры шрифтов
- Отступы и размеры элементов
- Анимации

## 🎯 Как это работает?

### Поток данных:
1. **App.jsx** управляет состоянием квиза (текущий вопрос, ответы, статус)
2. **Question.jsx** отображает текущий вопрос и варианты ответов
3. При клике на ответ:
   - Ответ сохраняется в `answers`
   - Переключаемся на следующий вопрос
   - Если вопросы закончились - переходим на экран результатов
4. **Results.jsx** показывает результат и рекомендацию

### Хранение ответов:
```javascript
answers = {
  1: 1,  // вопрос с id=1, выбран ответ с индексом 1
  2: 0,  // вопрос с id=2, выбран ответ с индексом 0
  3: 3,
  4: 2
}
```

## 📚 Комментарии в коде

Каждый файл содержит подробные комментарии, объясняющие:
- Что делает каждая функция
- Какие props используются компоненты
- Как работает логика

Отлично подходит для обучения!

## 🐛 Советы для начинающих

- Откройте DevTools браузера (F12) → Console, чтобы увидеть статус приложения
- Попробуйте добавить `console.log` в компоненты, чтобы понять поток данных
- Измените цвета в CSS и сразу увидите результат в браузере
- Используйте React Developer Tools для инспекции компонентов

## 🔮 Что можно добавить в V2

- Таймер на вопрос
- Сохранение прогресса в localStorage
- Категории квизов
- Разные типы вопросов (множественный выбор, текстовый ввод)
- Рейтинг сложности
- Обратная связь по выбранному ответу
- Экспорт результатов

---

**Хорошего обучения! 🚀**
